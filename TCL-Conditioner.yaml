substitutions:
  device_name: tclac
  humanly_name: TCL Air Conditioner
  wifi_ssid: "TOTOLINK ND300"
  wifi_password: "A777763945A"
  recovery_pass: mKSzmBdt7DAj
  ota_pass: 90cf525a594c8bb2cbf917bc2c2fa058
  api_key: lmUoWsWs1oqSfJqLFsA6PisHSx4Y1G+iu7V3PDIqpXk=
  uart_rx: GPIO16
  uart_tx: GPIO17
  receive_led: GPIO6
  transmit_led: GPIO4

esphome:
  name: ${device_name}
  friendly_name: ${humanly_name}

# ضبط الشريحة ESP32
esp32:
  board: esp32dev
  framework:
    type: arduino

# 1. تعريف منفذ السيريال المباشر باسطوانة ID معرفة (uart_bus)
uart:
  id: uart_bus
  rx_pin: ${uart_rx}
  tx_pin: ${uart_tx}
  baud_rate: 9600

# 2. التحديث اللاسلكي (OTA)
ota:
  - platform: esphome
    password: ${ota_pass}

# 3. الحزمة الأساسية للمكيف (تبني كارت المناخ التفاعلي تلقائياً)
packages:
  remote_package:
    url: https://github.com/sorz2122/tclac.git
    ref: master
    files:
      - packages/core.yaml
    refresh: 30s

# 4. إضافة قائمة وضع المولد (Generator Mode) المبرمجة
select:
  - platform: template
    name: "وضع المولد (Generator)"
    id: generator_mode_select
    icon: "mdi:generator-portable"
    options:
      - "Off"
      - "Level 1"
      - "Level 2"
      - "Level 3"
    initial_option: "Off"
    optimistic: true
    set_action:
      - lambda: |-
          uint8_t genLevel = 0;
          if (x == "Level 1") genLevel = 1;
          else if (x == "Level 2") genLevel = 2;
          else if (x == "Level 3") genLevel = 3;

          // بناء حزمة الـ 38 بايت المعتمدة لديكم
          uint8_t dataTX[38] = {0};
          dataTX[0] = 0xBB;
          dataTX[1] = 0x00;
          dataTX[2] = 0x01;
          dataTX[3] = 0x03;
          dataTX[4] = 0x20;
          dataTX[5] = 0x03;
          dataTX[6] = 0x01;
          dataTX[7] = 0x64;
          dataTX[8] = 0x03;
          dataTX[9] = 31 - 20;
          dataTX[10] = 0x00;
          dataTX[13] = 0x01;

          // الخانة 18 الخاصة بمستوى المولد
          dataTX[18] = genLevel;

          // حساب XOR Checksum
          uint8_t crc = 0;
          for (int i = 0; i < 37; i++) {
            crc ^= dataTX[i];
          }
          dataTX[37] = crc;

          // الإرسال عبر UART
          id(uart_bus).write_array(dataTX, 38);
          
