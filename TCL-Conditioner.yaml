substitutions:
  # Unique name for this configuration
  device_name: uneva_ac
  # Human-readable name for the interface
  humanly_name: Uneva Air Conditioner
  # WiFI settings
  wifi_ssid: "TOTOLINK ND300"
  wifi_password: "A777763945A"
  # Passwords and Keys
  recovery_pass: mKSzmBdt7DAj
  ota_pass: 90cf525a594c8bb2cbf917bc2c2fa058
  api_key: lmUoWsWs1oqSfJqLFsA6PisHSx4Y1G+iu7V3PDIqpXk=
  # UART and LED Pins
  uart_rx: GPIO16
  uart_tx: GPIO17
  receive_led: GPIO6
  transmit_led: GPIO4

# Remote repository files
packages:
  remote_package:
    url: https://github.com/I-am-nightingale/tclac.git
    ref: master
    files:
      - packages/core.yaml
    refresh: 30s

# Microcontroller specs
esp32:
  board: esp32dev
  framework:
    type: arduino

# ==========================================
# إضافة ميزة واجهة الويب للتحديث اللاسلكي عبر المتصفح
# ==========================================
web_server:
  port: 80

# ==========================================
# إضافة وضع المولد الذكي للتحكم في الأمبير
# ==========================================
select:
  - platform: template
    name: "وضع المولد (Gen Mode)"
    id: gen_mode_selector
    icon: "mdi:generator-portable"
    options:
      - "إيقاف (Off)"
      - "المستوى 1 (L1 - 80%)"
      - "المستوى 2 (L2 - 70%)"
      - "المستوى 3 (L3 - 50%)"
    optimistic: true
    set_action:
      lambda: |-
        if (x == "إيقاف (Off)") {
          id(uneva_ac).set_gen_mode(0);
        } else if (x == "المستوى 1 (L1 - 80%)") {
          id(uneva_ac).set_gen_mode(1);
        } else if (x == "المستوى 2 (L2 - 70%)") {
          id(uneva_ac).set_gen_mode(2);
        } else if (x == "المستوى 3 (L3 - 50%)") {
          id(uneva_ac).set_gen_mode(3);
        }
        
