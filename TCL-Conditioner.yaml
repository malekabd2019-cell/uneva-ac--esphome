substitutions:
# Уникальное имя этой конфигурации
  device_name: uneva-ac
# Человеческое имя для кондиционера, для интерфейса:
  humanly_name: Uneva Air Conditioner
# Настройки подключения к WiFI
# Имя точки доступа WiFI:
  wifi_ssid: "TOTOLINK ND300"
# Пароль точки доступа WiFI:
  wifi_password: "A777763945A"
# Пароль для точки доступа для восстановления, имя точки доступа будет: "{device_name} Fallback Hotspot"
  recovery_pass: mKSzmBdt7DAj
# Пароль для обновления по воздуху
  ota_pass: 90cf525a594c8bb2cbf917bc2c2fa058
# Ключ подключения к Home Assistant API
  api_key: lmUoWsWs1oqSfJqLFsA6PisHSx4Y1G+iu7V3PDIqpXk=
# Выводы модуля, к которым подключен UART (تعديل المداخل هنا):
  uart_rx: GPIO16
  uart_tx: GPIO17
# Выводы модуля для подключения сигнальных светодиодов
  receive_led: GPIO6
  transmit_led: GPIO4

# Подгружаемые файлы
packages:
  remote_package:
    url: https://github.com/I-am-nightingale/tclac.git
    ref: master
    files:
      - packages/core.yaml # Ядро всего сущего
    refresh: 30s

# Выбранный модуль (تفعيل معالج الـ ESP32 الخاص بك)
esp32:
  board: esp32dev
  framework:
    type: arduino

# ESP-01S (تم إيقافه لأن قطعتك هي ESP32)
#esp8266:
#  board: esp01_1m
